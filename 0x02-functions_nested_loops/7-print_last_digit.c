#include "main.h"

/**
 * print_last_digit - last digit
 * @n: is the letter being tested
 *
 * Return: Always 4`
 */

int print_last_digit(int n)
{
	int r;

	if (n >= 0)
	{
		r = ((n % 10) + 48);
		_putchar (r);
	}
	else
	}
		r = (-(n % 10) + 48);
		_putchar (0 + r);
	}
	return (4);
}
